8086以偶地址访问存储器
$表示当前地址
BYTE PTR一字节访问
在8086体系结构里，专门规定CX寄存器就是做“循环次数控制”的寄存器。
INC BX	BX向后移动1字节
处理器的主要功能是，实现所有指令的执行并处理数据
Hex是 十六进制的缩写
带符号的十六进制转换十进制方法：先将十六进制用二进制形式表示 88H=10001000B ，
然后按位取反，末位加 1 得 01111000，再转换为十进制得 -120
NEG DH 是一条 汇编指令，用于将寄存器DH 的值取负，即将DH的值变成其补码形式
PSW（即 Flags 寄存器）
1字=2字节
BX、BP、SI、DI是专门用来做寻址的，提供偏移地址
SHL AL, CL
SHL 是逻辑左移，AL左移CL位。
执行“DIV BX”指令后，AX寄存器中存放结果,DX寄存器存放商
DEC 让寄存器或内存的值减1
cf有符号运算的进位灯
of无符号运算的溢出灯
算术右移(SAR)保持符号位不变，高位空出的位用原来的符号位填充
将带符号位的机器数对应的真正数值称为机器数的真值。
正数的补码还是正数，[X]-[Y]=[X]+[Y]补码
cmp前一个小于后一个就是1
mov dx, 96会被放在底位
在计算机中，寄存器的位编号通常是从最低位（最右边）开始，从 0 开始编号。
OFFSET VAR1 返回的是变量 VAR1 的偏移地址，这是一个 16 位的值
AND 运算符的优先级高于 OR 运算符。
GE大于等于 LE小于等于
MIPS即“每秒百万条指令”
2K × 4 表示这个 RAM 芯片有 2K 个地址（2048 行），每个地址有 4 位数据（=0.5字节）
BX 默认使用数据段寄存器 DS，而 BP 默认使用堆栈段寄存器 SS
